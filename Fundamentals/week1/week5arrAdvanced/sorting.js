function sorting(arr) {

    let ascendingArr = arr.sort((a,b)=> a-b);
    // console.log(ascendingArr);
    // let descendingArr = arr.sort((a,b)=> b-a);
    // // console.log(descendingArr);

    // let newArr = [];

    // for(i = 0; i < arr.length / 2; i++) {
    //     let bigNum = ascendingArr[arr.length - 1 - i];
    //     let smallNum = ascendingArr[i];
    //     newArr.push(bigNum, smallNum);
    // }

    // let print = newArr.slice(0, arr.length);
    // console.log(newArr.join(" "));

    for(let i = 0; i < arr.length; i++) {

    }
 
}
sorting([1, 21, 3, 52, 69, 63, 31,
    2, 18, 94])